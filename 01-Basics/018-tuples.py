t = ('one','two','three', 'one') # Create a simple tuple

# t[0] # Get index 0 element

# len(t) # Length of tuple in our case it returns 3

# t[-1] # That returns three in our case because three is on index -1

# t.index('one') # Gives you index number

# t.count('one') # Return number of elements

print(t.count('one'))